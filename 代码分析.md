# 实现逻辑

## 1. 项目概述

这是一个基于 **SFML 3.x** 实现的经典 **打砖块（Breakout）** 游戏。程序采用单文件架构，通过结构体封装游戏实体，使用基于帧时间（Delta Time）的游戏循环驱动更新，实现了挡板控制、球体运动、AABB 碰撞检测、物理反弹及胜负判定等核心机制。

---

## 2. 常量配置

| 常量 | 值 | 说明 |
|------|-----|------|
| `WINDOW_WIDTH` / `WINDOW_HEIGHT` | 800 / 600 | 游戏窗口分辨率（像素） |
| `PADDLE_WIDTH` / `PADDLE_HEIGHT` | 100.0f / 20.0f | 挡板尺寸 |
| `PADDLE_SPEED` | 500.0f | 挡板移动速度（像素/秒） |
| `BALL_RADIUS` | 10.0f | 球的半径 |
| `BALL_SPEED` | 300.0f | 球的基础移动速度（像素/秒） |
| `BRICK_WIDTH` / `BRICK_HEIGHT` | 70.0f / 25.0f | 单个砖块的尺寸 |
| `BRICK_COLS` / `BRICK_ROWS` | 10 / 5 | 砖块阵列的列数与行数 |

---

## 3. 核心数据结构

### 3.1 方向枚举 `Direction`

```cpp
enum class Direction { LEFT, RIGHT, NONE };
```

用于表示挡板的当前移动方向，`NONE` 表示静止。

### 3.2 砖块 `Brick`

```cpp
struct Brick {
    sf::RectangleShape shape;  // SFML 矩形，负责渲染
    bool active = true;        // 存活标记
};
```

- **职责**：作为静态障碍物存在。
- **渲染特性**：具有黑色 1 像素边框，每行颜色不同（彩虹色）。
- **生命周期**：被球击中时 `active` 置为 `false`，不再参与碰撞和绘制。

### 3.3 挡板 `Paddle`

```cpp
struct Paddle {
    sf::RectangleShape shape;
    Direction direction = Direction::NONE;
    void update(float dt);
};
```

- **初始化位置**：窗口底部中央，距离底部 30 像素。
- **`update(float dt)` 逻辑**：
    1. 根据 `direction` 向左或向右移动：`位移 = PADDLE_SPEED × dt`。
    2. **边界钳制（Clamp）**：左边缘 `x >= 0`，右边缘 `x + width <= WINDOW_WIDTH`，防止挡板移出屏幕。

### 3.4 球 `Ball`

```cpp
struct Ball {
    sf::CircleShape shape;
    sf::Vector2f velocity;     // 速度向量 (vx, vy)
    bool attached = true;      // 是否附着在挡板上
    void reset(...);
    void launch();
    void update(float dt, Paddle& paddle);
};
```

#### `reset(const sf::Vector2f& paddleCenter)`
将球重置到挡板正上方，速度归零，重新标记为 `attached = true`。用于游戏初始化或球掉落底部后的重置。

#### `launch()`
将球从挡板发射出去：
1. 生成随机水平偏移量 `xDir ∈ [-1.0, 1.0]`。
2. 设置速度向量：`velocity = { xDir * BALL_SPEED, -BALL_SPEED }`。
    - `vy = -BALL_SPEED` 确保球始终向上飞出。
    - 速度大小范围：`[BALL_SPEED, √2 * BALL_SPEED]`。

#### `update(float dt, Paddle& paddle)`
分两种状态处理：

**状态 A：`attached == true`（未发射）**
- 球的位置实时跟随挡板中心正上方：`ball.y = paddle.y - BALL_RADIUS`。

**状态 B：`attached == false`（飞行中）**
- **位置更新**：`pos = pos + velocity × dt`。
- **左右边界反弹**：当 `pos.x ± radius` 触及窗口左右边缘时，`velocity.x = -velocity.x`，并用 `std::clamp` 修正位置防止穿透。
- **顶部边界反弹**：当 `pos.y - radius <= 0` 时，`velocity.y = -velocity.y`。
- **底部掉落检测**：当 `pos.y - radius > WINDOW_HEIGHT` 时，调用 `reset()` 重置球。

---

## 4. 碰撞检测系统

### 4.1 AABB 检测 `intersects`

```cpp
bool intersects(const sf::FloatRect& a, const sf::FloatRect& b) {
    return a.findIntersection(b).has_value();
}
```

利用 SFML 3.x 的 `findIntersection` 判断两个矩形是否有交集，返回 `std::optional<sf::FloatRect>`，通过 `has_value()` 判断是否碰撞。

### 4.2 挡板碰撞（物理反弹模型）

**触发条件**：
- 球处于飞行状态 (`!attached`)
- 球向下运动 (`velocity.y > 0`)
- 球与挡板发生 AABB 碰撞

**反弹算法**：
1. 计算击球点相对于挡板中心的水平偏移：`hitPoint = ball.x - paddle_center.x`。
2. 归一化：`normalizedHit = hitPoint / (PADDLE_WIDTH / 2.0f)`，范围 `[-1, 1]`。
3. 计算新速度：
    - `vx' = normalizedHit × BALL_SPEED` （击中中心时水平速度为 0，击中两端时为 ±300）
    - `vy' = -|vy|` （强制向上反弹）

**物理意义**：该模型模拟了挡板不同位置对球方向的控制效果——击中中心垂直反弹，击中边缘大角度偏转。

### 4.3 砖块碰撞

**触发条件**：球与激活状态的砖块发生 AABB 碰撞。

**处理逻辑**：
- 砖块 `active = false`（标记销毁）。
- 球垂直速度取反：`velocity.y = -velocity.y`。
- 得分增加 10 分。
- `break` 退出当前帧的砖块循环（一帧仅处理一次碰撞，避免单帧内多次反弹导致物理异常）。

---

## 5. 主函数 `main()` 逻辑流程

### 5.1 初始化阶段

1. **创建窗口**：`sf::RenderWindow`，分辨率 800×600，标题 "SFML Breakout Demo"，限制 60 FPS。
2. **创建实体**：
    - `Paddle paddle`
    - `Ball ball`（并调用 `reset` 放置于挡板上方）
3. **初始化砖块阵列**：
    - 计算起始 X 坐标使阵列水平居中：`startX = (WIDTH - COLS * BRICK_WIDTH) / 2`。
    - 双层循环生成 `5 行 × 10 列` 砖块，Y 方向从顶部 50 像素处开始。
    - 每行赋予不同颜色（红、橙、黄、绿、蓝）。
4. **初始化状态**：`score = 0`，`gameWon = false`。

### 5.2 游戏主循环 `while (window.isOpen())`

#### 步骤 1：计算帧时间
```cpp
float dt = clock.restart().asSeconds();
```
获取上一帧到当前帧的秒数，确保所有运动基于时间而非帧率，保证在不同性能机器上速度一致。

#### 步骤 2：事件处理 `pollEvent()`
- **窗口关闭事件**：调用 `window.close()`。
- **按键事件**：
    - **空格键**：调用 `ball.launch()` 发射球。
    - **R 键**：重置游戏——分数清零、重新生成挡板和球、恢复所有砖块。

#### 步骤 3：实时输入检测 `isKeyPressed()`
检测 **A/D** 或 **←/→** 方向键，设置 `paddle.direction`：
- 同时按下多个键时，优先级：`LEFT` > `RIGHT` > `NONE`（因为使用 `if-else if` 链式判断）。

#### 步骤 4：游戏逻辑更新（仅 `!gameWon` 时执行）

1. **`paddle.update(dt)`**：根据方向移动挡板。
2. **`ball.update(dt, paddle)`**：更新球位置，处理边界碰撞和掉落重置。
3. **挡板碰撞检测**：执行上述挡板反弹物理模型。
4. **砖块碰撞检测**：遍历所有砖块，执行碰撞销毁和反弹逻辑。
5. **胜负判定**：遍历砖块检查是否 `active == true`。若全部销毁，`gameWon = true`。游戏状态冻结，不再更新物理。

#### 步骤 5：绘制阶段

```cpp
window.clear(sf::Color::Black);   // 清屏（黑色背景）
// 绘制所有激活的砖块
for (const auto& brick : bricks) {
    if (brick.active) window.draw(brick.shape);
}
window.draw(paddle.shape);        // 绘制挡板
window.draw(ball.shape);          // 绘制球
window.display();                 // 双缓冲交换，显示到屏幕
```

---

## 6. 游戏机制总结

| 功能 | 实现方式 |
|------|----------|
| **挡板控制** | 键盘 A/D 或方向键，基于时间的平滑移动 |
| **球的发射** | 空格键，随机水平角度的向上发射 |
| **边界反弹** | 碰到左右上边界时反转对应速度分量 |
| **挡板反弹** | 基于击球点水平偏移的归一化速度重定向 |
| **砖块消除** | AABB 碰撞检测，击中后 `active = false` |
| **掉落重置** | 球掉出底部窗口后自动重置到挡板上方 |
| **游戏重置** | R 键恢复所有实体到初始状态 |
| **胜利判定** | 所有砖块 `active == false` 时 `gameWon = true` |

---

## 7. 代码亮点

1. **基于 Delta Time 的动画**：所有移动均乘以 `dt`，确保在不同帧率下运动速度一致。
2. **SFML 3.x 现代 API**：使用 `std::optional` 风格的事件处理（`event->is<sf::Event::Closed>()`）和 `findIntersection`。
3. **物理简化但有效**：挡板反弹模型用简单的线性映射实现了富有趣味性的角度控制，避免了复杂向量反射计算。
4. **单帧单碰撞**：砖块碰撞使用 `break` 限制一帧只处理一个砖块，防止球在砖块缝隙中高速震荡。
