# SFML Breakout 游戏 — 伪代码结构说明

> 本文档按原代码的方法调用结构，以中文注释形式描述程序的执行流程与逻辑。

---

## 一、全局常量配置

```伪代码
定义 窗口宽度 = 800 像素
定义 窗口高度 = 600 像素

定义 挡板宽度 = 100.0 像素
定义 挡板高度 = 20.0 像素
定义 挡板移动速度 = 500.0 像素/秒

定义 球半径 = 10.0 像素
定义 球移动速度 = 300.0 像素/秒

定义 砖块宽度 = 70.0 像素
定义 砖块高度 = 25.0 像素
定义 砖块列数 = 10
定义 砖块行数 = 5
```

---

## 二、方向枚举

```伪代码
枚举 方向 {
    左移,
    右移,
    无移动
}
```

---

## 三、砖块类 (Brick)

```伪代码
类 砖块:
    成员: 矩形图形对象
    成员: 是否激活 = 真

    构造函数(位置X, 位置Y, 颜色):
        设置矩形尺寸为(砖块宽度, 砖块高度)
        设置填充颜色 = 传入颜色
        设置边框颜色 = 黑色
        设置边框粗细 = 1像素
        设置矩形位置 = (位置X, 位置Y)
```

---

## 四、挡板类 (Paddle)

```伪代码
类 挡板:
    成员: 矩形图形对象
    成员: 当前方向 = 无移动

    构造函数():
        设置矩形尺寸为(挡板宽度, 挡板高度)
        设置填充颜色 = 白色
        设置初始位置:
            X = (窗口宽度 - 挡板宽度) / 2      // 水平居中
            Y = 窗口高度 - 挡板高度 - 30        // 底部留30像素间距

    方法 更新(时间差dt):
        如果 当前方向 == 左移:
            获取当前位置
            向左移动: 位置X -= 挡板移动速度 × 时间差
            限制位置X不小于0（左边界）
            更新位置
        否则如果 当前方向 == 右移:
            获取当前位置
            向右移动: 位置X += 挡板移动速度 × 时间差
            限制位置X不超过窗口右边缘（右边界）
            更新位置
```

---

## 五、球类 (Ball)

```伪代码
类 球:
    成员: 圆形图形对象
    成员: 速度向量(x分量, y分量)
    成员: 是否附着在挡板上 = 真

    构造函数():
        设置圆半径 = 球半径
        设置填充颜色 = 青色
        设置原点为圆心(用于旋转/缩放)

    方法 重置(挡板中心位置):
        将球位置设置到挡板中心正上方
        速度归零
        标记为附着在挡板上

    方法 发射():
        如果 已经发射: 返回
        标记为未附着（已发射）
        生成随机水平方向偏移量(-1.0 ~ 1.0)
        设置速度向量:
            X方向 = 偏移量 × 球速度
            Y方向 = -球速度（向上）

    方法 更新(时间差dt, 挡板引用):
        如果 球仍附着在挡板上:
            球位置 = 挡板中心正上方
            // 球X = 挡板X + 挡板宽度/2
            // 球Y = 挡板Y - 球半径
            返回

        // 根据速度和时间更新位置
        新位置 = 原位置 + 速度 × 时间差

        // 左右边界反弹
        如果 球碰到左墙或右墙:
            水平速度取反 (X方向速度 = -X方向速度)
            校正球位置到边界内

        // 顶部边界反弹
        如果 球碰到天花板:
            垂直速度取反 (Y方向速度 = -Y方向速度)
            校正球位置到边界内

        // 底部检测（掉落惩罚）
        如果 球掉出屏幕底部:
            调用重置(挡板中心位置)
            返回

        更新球位置
```

---

## 六、碰撞检测函数

```伪代码
函数 是否相交(矩形A, 矩形B):
    返回 矩形A与矩形B是否存在交集
```

---

## 七、主函数 (main)

```伪代码
函数 主函数():

    // ==================== 初始化阶段 ====================

    创建渲染窗口:
        尺寸 = (窗口宽度, 窗口高度)
        标题 = "SFML Breakout Demo"
        样式 = 仅显示关闭按钮

    限制窗口帧率 = 60 FPS

    创建挡板对象

    创建球对象
    调用球.重置(挡板中心正上方)

    // 创建砖块阵列
    初始化砖块列表
    定义每行颜色 = [红, 橙, 黄, 绿, 蓝]
    计算起始X坐标使砖块水平居中
    对于 行 从 0 到 行数-1:
        对于 列 从 0 到 列数-1:
            计算砖块位置 = (起始X + 列×砖块宽度, 50 + 行×砖块高度)
            创建砖块(位置, 颜色[行])
            加入砖块列表

    初始化游戏状态:
        得分 = 0
        是否获胜 = 假

    创建时钟对象(用于计算帧间隔时间)


    // ==================== 主游戏循环 ====================

    循环执行(只要窗口未关闭):

        // 1. 获取帧间隔时间（秒）
        时间差 = 时钟重新开始计时.转为秒


        // 2. 事件处理
        当 有事件待处理 时:
            如果 事件类型是 关闭窗口:
                关闭窗口

            如果 事件类型是 键盘按下:
                如果 按键是 空格键:
                    调用球.发射()

                如果 按键是 R键:
                    重置游戏:
                        得分 = 0
                        是否获胜 = 假
                        重新创建挡板
                        调用球.重置(挡板中心正上方)
                        恢复所有砖块为激活状态


        // 3. 实时输入检测
        如果 A键或左方向键被按下:
            挡板方向 = 左移
        否则如果 D键或右方向键被按下:
            挡板方向 = 右移
        否则:
            挡板方向 = 无移动


        // 4. 更新游戏逻辑
        如果 未获胜:
            调用挡板.更新(时间差)
            调用球.更新(时间差, 挡板)

            如果 球已发射(未附着在挡板):
                // 球与挡板碰撞检测
                如果 球向下运动 且 球矩形与挡板矩形相交:
                    计算击中点 = 球X - 挡板中心X
                    归一化击中点 = 击中点 / (挡板宽度/2)  // 范围 [-1, 1]
                    球的水平速度 = 归一化击中点 × 球速度    // -300 ~ +300
                    球的垂直速度 = -|当前垂直速度|            // 确保向上反弹

                // 球与砖块碰撞检测
                遍历 每个砖块:
                    如果 砖块激活 且 球与砖块相交:
                        标记砖块为击碎(激活 = 假)
                        球的垂直速度取反(反弹)
                        得分 += 10
                        跳出循环(一帧只处理一次碰撞)

                // 检查是否获胜
                是否获胜 = 真
                遍历 每个砖块:
                    如果 有砖块仍然激活:
                        是否获胜 = 假
                        跳出循环


        // 5. 绘制阶段
        清空画布(填充黑色背景)

        遍历 每个砖块:
            如果 砖块激活:
                绘制砖块

        绘制挡板
        绘制球

        显示渲染结果(双缓冲交换)


    返回 0
```

---

## 八、方法调用关系图

```
main()
├── 创建 Paddle()
│   └── 设置矩形尺寸、颜色、位置
│
├── 创建 Ball()
│   └── 设置圆半径、颜色、原点
│
├── ball.reset()
│   └── 设置位置、速度归零、标记附着
│
├── 创建 Brick() × 50
│   └── 设置矩形尺寸、颜色、边框、位置
│
└── 主循环
    ├── clock.restart() → 获取时间差
    ├── pollEvent() → 事件处理
    │   ├── KeyPressed::Space → ball.launch()
    │   │   └── 设置速度向量、标记脱离
    │   └── KeyPressed::R → 重置游戏
    │       ├── score = 0
    │       ├── paddle = Paddle()
    │       ├── ball.reset()
    │       └── brick.active = true
    │
    ├── Keyboard::isKeyPressed() → 设置 paddle.direction
    │
    ├── paddle.update(dt)
    │   └── 根据方向移动、边界限制
    │
    ├── ball.update(dt, paddle)
    │   ├── 如果附着: 跟随挡板
    │   ├── 更新位置 (pos += velocity × dt)
    │   ├── 左右边界反弹
    │   ├── 顶部边界反弹
    │   └── 底部掉落 → ball.reset()
    │
    ├── intersects(球, 挡板)
    │   └── 计算碰撞反弹速度
    │
    ├── intersects(球, 砖块)
    │   └── 标记砖块击碎、速度反弹、加分
    │
    └── 绘制
        ├── window.draw(砖块)
        ├── window.draw(挡板)
        ├── window.draw(球)
        └── window.display()
```

---

## 九、核心算法公式总结

| 功能 | 公式/逻辑 |
|------|----------|
| 位移更新 | `新位置 = 原位置 + 速度 × 时间差` |
| 挡板边界限制 | `位置 = clamp(位置, 0, 窗口宽度 - 挡板宽度)` |
| 左右墙壁反弹 | `水平速度 = -水平速度` |
| 顶部天花板反弹 | `垂直速度 = -垂直速度` |
| 挡板反弹（水平） | `vx = (击中点 / 半宽) × 球速` |
| 挡板反弹（垂直） | `vy = -abs(vy)` |
| 砖块反弹 | `vy = -vy` |
| AABB碰撞检测 | `矩形A.findIntersection(矩形B).has_value()` |
